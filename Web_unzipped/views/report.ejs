<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Report User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex flex-col items-center py-10">
  <div class="bg-gray-800 p-8 rounded-2xl shadow-lg w-full max-w-md text-center">
    <h1 class="text-2xl font-bold text-red-400 mb-6">Report User</h1>
    <form action="/report/<%= reportedUser._id %>" method="POST" class="space-y-4 text-left">
      <div>
        <label class="block text-sm font-medium mb-1">User:</label>
        <div class="font-semibold text-pink-300"><%= reportedUser.name %> (<%= reportedUser.email %>)</div>
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="reason">Reason for Report</label>
        <select name="reason" id="reason" required class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900">
          <option value="">Select a reason</option>
          <option value="Spam">Spam</option>
          <option value="Harassment">Harassment</option>
          <option value="Inappropriate Content">Inappropriate Content</option>
          <option value="Fake Profile">Fake Profile</option>
          <option value="Other">Other</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="details">Details (optional)</label>
        <textarea name="details" id="details" rows="3" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900"></textarea>
      </div>
      <button type="submit" class="w-full bg-red-500 text-white font-semibold py-3 rounded-xl shadow-md hover:bg-red-600 transition">Submit Report</button>
    </form>
    <a href="/dashboard" class="block mt-6 text-indigo-400 hover:underline text-sm">← Back to Dashboard</a>
  </div>
</body>
</html>
