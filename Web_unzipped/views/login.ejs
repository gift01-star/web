<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Inter&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body, html { height: 100%; font-family: 'Inter', sans-serif; }
    .landing-container {
      background-image: linear-gradient(120deg, rgba(236,72,153,0.7) 0%, rgba(139,92,246,0.7) 100%), url('https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=1470&q=80');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      position: relative;
    }
    .overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(120deg, rgba(236,72,153,0.45) 0%, rgba(139,92,246,0.45) 100%);
      z-index: 0;
      opacity: 0.7;
      animation: fadeIn 1.5s;
    }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 0.85; } }
    .form-card {
      position: relative;
      z-index: 1;
      background: rgba(255,255,255,0.92);
      border-radius: 18px;
      box-shadow: 0 2px 24px 0 rgba(139,92,246,0.08);
      padding: 48px 32px 32px;
      max-width: 400px;
      width: 100%;
      margin: 32px 16px;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .form-card h1 {
      font-size: 2.2rem;
      font-family: 'Playfair Display', serif;
      color: #ec4899;
      margin-bottom: 0.5rem;
    }
    .form-card p {
      color: #6d28d9;
      margin-bottom: 1.5rem;
      font-size: 1.1rem;
    }
    .form-card form {
      width: 100%;
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
      text-align: left;
    }
    .form-card label {
      font-size: 1rem;
      color: #6b7280;
      margin-bottom: 0.3rem;
      display: block;
    }
    .form-card input {
      width: 100%;
      padding: 0.75rem 1rem;
      border-radius: 1rem;
      border: 1px solid #d1d5db;
      font-size: 1rem;
      outline: none;
      margin-bottom: 0.2rem;
      transition: border 0.2s, box-shadow 0.2s;
    }
    .form-card input:focus {
      border: 1.5px solid #ec4899;
      box-shadow: 0 0 0 2px #fbcfe8;
    }
    .form-card button {
      background: linear-gradient(90deg, #ec4899 0%, #8b5cf6 100%);
      color: #fff;
      font-weight: 600;
      border: none;
      border-radius: 1rem;
      padding: 0.9rem 0;
      font-size: 1.1rem;
      cursor: pointer;
      margin-top: 0.5rem;
      transition: background 0.2s;
    }
    .form-card button:hover {
      background: linear-gradient(90deg, #db2777 0%, #7c3aed 100%);
    }
    .form-card .error {
      background: #fee2e2;
      color: #b91c1c;
      border-radius: 0.5rem;
      padding: 0.5rem 1rem;
      margin-bottom: 1rem;
      font-size: 0.95rem;
      text-align: center;
    }
    .form-card .text-right { text-align: right; }
    .form-card .mt-6 { margin-top: 1.5rem; }
    .form-card .text-sm { font-size: 0.95rem; }
    .form-card .text-pink { color: #ec4899; }
    @media (max-width: 600px) {
      .form-card { padding: 24px 8px 16px; }
      .form-card h1 { font-size: 1.5rem; }
    }
  </style>
</head>
<body>
  <div class="landing-container">
    <div class="overlay"></div>
    <div class="form-card">
      <h1>Login</h1>
      <p>Find love, one click at a time</p>
      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } %>
      <form action="/login" method="POST">
        <div>
          <label for="email">Email</label>
          <input type="email" name="email" id="email" placeholder="you@example.com" required />
        </div>
        <div>
          <label for="password">Password</label>
          <input type="password" name="password" id="password" placeholder="••••••••" required />
        </div>
        <div class="text-right">
          <a href="/reset-password" style="font-size:0.95rem;color:#ec4899;">Forgot password?</a>
        </div>
        <button type="submit">Login</button>
      </form>
      <p class="mt-6 text-sm">Don't have an account? <a href="/register-step1" class="text-pink">Register</a></p>
    </div>
  </div>
</body>




  <script src="/js/location.js"></script>
  <script>
    document.querySelector('form').addEventListener('submit', function(e) {
      if (navigator.geolocation) {
        e.preventDefault();
        navigator.geolocation.getCurrentPosition(function(position) {
          const lat = position.coords.latitude;
          const lng = position.coords.longitude;
          fetch('/api/location', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ latitude: lat, longitude: lng })
          }).finally(() => {
            e.target.submit();
          });
        }, function() {
          e.target.submit();
        });
      }
    });
  </script>
</body>
</html>
