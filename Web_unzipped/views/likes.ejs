<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Likes | SoulSwipe</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <style>
    .card-hover {
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }
    .card-hover:hover {
      transform: translateY(-5px) scale(1.02);
      box-shadow: 0 8px 20px rgba(255, 105, 180, 0.4);
    }
  </style>
</head>
<body class="bg-gradient-to-b from-purple-900 via-gray-900 to-black text-white min-h-screen">

  <!-- Navbar -->
  <nav class="bg-purple-600 p-4 flex justify-between items-center shadow-lg">
    <div class="text-2xl font-extrabold tracking-wider">Likes</div>
    <a href="/dashboard" class="text-yellow-300 hover:text-white font-semibold">Dashboard</a>
  </nav>

  <div class="max-w-4xl mx-auto p-6">
    <h2 class="text-3xl mb-6 font-bold text-pink-300">People Who Liked You</h2>
    <% if (likes.length === 0) { %>
      <p class="text-center text-gray-400">No likes yet. Keep exploring!</p>
    <% } %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      <% likes.forEach(user => { %>
        <div class="bg-gray-800 rounded-2xl shadow-lg card-hover flex flex-col items-center p-5">
          <div class="relative w-28 h-28 mb-3">
            <img src="<%= (user.image && user.image !== '' ? (user.image.startsWith('/') ? user.image : '/uploads/' + user.image) : '/uploads/default.jpg') %>" alt="<%= user.name %>" class="w-28 h-28 rounded-full object-cover border-2 border-pink-300" />
          </div>
          <h3 class="text-lg font-bold text-pink-200 mb-1"><%= user.name %></h3>
        </div>
      <% }) %>
    </div>
  </div>

</body>
</html>