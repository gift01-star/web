<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile - <%= user.name %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex flex-col items-center py-10">
  <div class="bg-gray-800 p-8 rounded-2xl shadow-lg w-full max-w-md text-center">
    <h1 class="text-2xl font-bold text-pink-300 mb-6">Edit Profile</h1>
    <form action="/profile/edit" method="POST" enctype="multipart/form-data" class="space-y-4 text-left">
      <div>
        <label class="block text-sm font-medium mb-1" for="name">Name</label>
        <input type="text" name="name" id="name" value="<%= user.name %>" required class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900 focus:ring-2 focus:ring-pink-400 focus:outline-none" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="age">Age</label>
        <input type="number" name="age" id="age" value="<%= user.age || '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900 focus:ring-2 focus:ring-pink-400 focus:outline-none" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="gender">Gender</label>
        <select name="gender" id="gender" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900">
          <option value="" <%= !user.gender ? 'selected' : '' %>>Select Gender</option>
          <option value="Male" <%= user.gender === 'Male' ? 'selected' : '' %>>Male</option>
          <option value="Female" <%= user.gender === 'Female' ? 'selected' : '' %>>Female</option>
          <option value="Other" <%= user.gender === 'Other' ? 'selected' : '' %>>Other</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="bio">Bio</label>
        <textarea name="bio" id="bio" rows="3" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900"><%= user.bio %></textarea>
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="interests">Interests (comma separated)</label>
        <input type="text" name="interests" id="interests" value="<%= user.interests ? user.interests.join(', ') : '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="hobbies">Hobbies (comma separated)</label>
        <input type="text" name="hobbies" id="hobbies" value="<%= user.hobbies ? user.hobbies.join(', ') : '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="religion">Religion</label>
        <input type="text" name="religion" id="religion" value="<%= user.religion || '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="preferredGender">Preferred Gender</label>
        <input type="text" name="preferredGender" id="preferredGender" value="<%= user.preferredGender || '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="instagram">Instagram</label>
        <input type="text" name="instagram" id="instagram" value="<%= user.social && user.social.instagram ? user.social.instagram : '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="twitter">Twitter</label>
        <input type="text" name="twitter" id="twitter" value="<%= user.social && user.social.twitter ? user.social.twitter : '' %>" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1" for="profilePicture">Profile Picture</label>
        <input type="file" name="profilePicture" id="profilePicture" accept="image/*" class="w-full px-4 py-2 rounded-xl border border-gray-600 bg-gray-900" />
      </div>
      <button type="submit" class="w-full bg-pink-500 text-white font-semibold py-3 rounded-xl shadow-md hover:bg-pink-600 transition">Save Changes</button>
    </form>
    <a href="/profile/<%= user._id %>" class="block mt-6 text-indigo-400 hover:underline text-sm">Cancel</a>
  </div>
  <script src="/js/location.js"></script>
  <script>
    document.querySelector('form').addEventListener('submit', function(e) {
      if (navigator.geolocation) {
        e.preventDefault();
        navigator.geolocation.getCurrentPosition(function(position) {
          const lat = position.coords.latitude;
          const lng = position.coords.longitude;
          fetch('/api/location', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ latitude: lat, longitude: lng })
          }).finally(() => {
            e.target.submit();
          });
        }, function() {
          e.target.submit();
        });
      }
    });
  </script>
</body>
</html>