<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Settings | PaKoNa</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-b from-pink-100 via-pink-200 to-pink-300">
  <div class="bg-white rounded-2xl shadow-lg p-8 w-full max-w-lg text-center">
    <h1 class="text-3xl font-bold text-pink-600 mb-2">Settings</h1>
    <p class="text-gray-500 mb-6">Manage your account, privacy, and preferences</p>
    <form method="POST" action="/update-settings" class="space-y-6 text-left">
      <!-- Profile Info -->
      <div>
        <h2 class="text-lg font-semibold text-pink-500 mb-2">Profile</h2>
        <label class="block text-sm font-medium text-gray-600 mb-1" for="displayName">Display Name</label>
        <input type="text" name="displayName" id="displayName" value="<%= user.name %>" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-pink-400 focus:outline-none mb-3" />
        <label class="block text-sm font-medium text-gray-600 mb-1" for="email">Email</label>
        <input type="email" name="email" id="email" value="<%= user.email %>" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-pink-400 focus:outline-none mb-3" />
        <label class="block text-sm font-medium text-gray-600 mb-1" for="bio">Bio</label>
        <textarea name="bio" id="bio" rows="2" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-pink-400 focus:outline-none mb-3"><%= user.bio %></textarea>
      </div>
      <!-- Password -->
      <div>
        <h2 class="text-lg font-semibold text-pink-500 mb-2">Change Password</h2>
        <input type="password" name="password" placeholder="New password" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-pink-400 focus:outline-none mb-3" />
      </div>
      <!-- Privacy -->
      <div>
        <h2 class="text-lg font-semibold text-pink-500 mb-2">Privacy</h2>
        <label class="flex items-center gap-2 mb-2">
          <input type="checkbox" name="showProfile" <%= user.showProfile ? 'checked' : '' %> class="accent-pink-500" />
          <span class="text-gray-700">Show my profile to others</span>
        </label>
        <label class="flex items-center gap-2 mb-2">
          <input type="checkbox" name="showOnline" <%= user.online ? 'checked' : '' %> class="accent-pink-500" />
          <span class="text-gray-700">Show when I'm online</span>
        </label>
      </div>
      <!-- Notifications -->
      <div>
        <h2 class="text-lg font-semibold text-pink-500 mb-2">Notifications</h2>
        <label class="flex items-center gap-2 mb-2">
          <input type="checkbox" name="emailNotifications" <%= user.emailNotifications ? 'checked' : '' %> class="accent-pink-500" />
          <span class="text-gray-700">Email notifications</span>
        </label>
        <label class="flex items-center gap-2 mb-2">
          <input type="checkbox" name="pushNotifications" <%= user.pushNotifications ? 'checked' : '' %> class="accent-pink-500" />
          <span class="text-gray-700">Push notifications</span>
        </label>
      </div>
      <!-- Subscription -->
      <div>
        <h2 class="text-lg font-semibold text-pink-500 mb-2">Subscription</h2>
        <p class="mb-2">Current plan: <span class="font-semibold"><%= user.isPremium ? 'Premium' : 'Free' %></span></p>
        <a href="/premium" class="inline-block bg-pink-500 text-white px-4 py-2 rounded-xl shadow hover:bg-pink-600 transition">Upgrade</a>
      </div>
      <!-- Account Actions -->
      <div class="mt-6 flex flex-col gap-2">
        <button type="submit" class="w-full bg-pink-500 text-white font-semibold py-3 rounded-xl shadow-md hover:bg-pink-600 transition">Save Changes</button>
        <a href="/logout" class="w-full bg-gray-300 text-gray-700 font-semibold py-3 rounded-xl shadow hover:bg-gray-400 transition">Logout</a>
        <a href="/delete-account" class="w-full bg-red-500 text-white font-semibold py-3 rounded-xl shadow hover:bg-red-600 transition">Delete Account</a>
      </div>
    </form>
  </div>
</body>
</html>
